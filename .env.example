# Required for POST /api/calls ingest endpoint
INGEST_API_KEY=your-secret-api-key-here

# Required for dashboard basic authentication
DASHBOARD_USER=admin
DASHBOARD_PASSWORD=password

# Optional: override data file location (default: ./data/calls.json)
# DATA_FILE_PATH=./data/calls.json
